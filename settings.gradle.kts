rootProject.name = "SourceScE"

plugins {
    // Lets Gradle auto-download the Java 25 toolchain if it isn't already installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}
